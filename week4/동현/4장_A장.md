# 유돈노jsyet 스터디

### 4장 리뷰

이전 단원들에 대한 리뷰

### A.1 값 vs 참조

call by value, reference 개념이 나오네요

Array, Object, Function의 경우엔 레퍼런스로 동작하는 예시가 나옵니다

### A.2 다양한 형태의 함수

익명함수 : 함수명 없이 선언

```jsx
const add = function(a, b) {
  return a + b;
};
```

기명함수(default)

```jsx
const add = function addFn(a, b) {
  return a + b;
};
```

### A.3 강제 조건부 비교

if 조건문 안에 변수를 넣을시 자동으로 == true 연산을 수행하는 걸 일컫는다.

== 이므로 예상하지 못한 동작이 나올 수 있음을 주의해야한다

### A.4 프로토타입 클래스

프로토타입을 활용해 메서드 추가하는것보단 이미 syntax sugar지만 동일하게 동작하고 위험한 내부구현이 막혀있는 class 문법을 사용하는게 정신건강에 이롭다.
