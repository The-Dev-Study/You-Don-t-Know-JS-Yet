const가 객체로 선언되면 그 안의 내용을 바꿀 수 있어 혼란이 발생하기 때문에 const를 사용해 객체를 정의하는건 피하도록 하자

-> 그럼 const 대신 어떻게 사용하는지 안나와있어 혼란스럽다. as const 또는 object.freeze처럼 readonly를 사용하는걸 권장하고 싶었던걸까

같음에 대한 고찰 -> 

```javascript
NaN === NaN // false
0 === -0;		// true
```

이렇게 설계된 원인은 무엇일까?

== 는 설계미스라고 밝힌 바가 있다는데 그렇다면 이제는 ==을 아예 쓰는 경우가 없는걸까? 유용한 케이스가 있을까?

ES 모듈이 싱글톤처럼 동작한다는 것은 처음 알았다. 근데 어차피 번들링하면서 모듈을 불러오는 것이라면 설정에 싱글톤처럼 동작하지 말라는 옵션도 있지 않을까? 없다면 왜일까?